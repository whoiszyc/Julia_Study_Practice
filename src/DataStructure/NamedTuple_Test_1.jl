# Define a named tuple (元组)
x=(a=1,b=2)

# indexing can be performed
x.a
x[:a]

#  A tuple of the names can be obtained using keys, and a tuple of the values can be obtained using values
keys(x)
values(x)
